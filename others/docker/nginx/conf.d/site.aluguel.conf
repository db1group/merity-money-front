server {
    listen 80;
    listen [::]:80;

    server_name aluguelnobolso.com.br www.aluguelnobolso.com.br;
    return 301 $scheme://use.aluguelnobolso.com.br$request_uri;

    access_log /var/log/nginx/prod-site.aluguelnobolso.com.br_access.log;
    error_log /var/log/nginx/prod-site.aluguelnobolso.com.br_error.log;
#
    #root /ANB/site/prod/code_new;
    #index index.php index.html index.htm;
#
    #location ~ /.well-known/acme-challenge {
    #    allow all;
    #    root /ANB/site/prod/code_new;
    #}
#
    #location / {
    #    try_files $uri $uri/ /index.php$is_args$args;
    #}
#
    #location ~ /\.ht {
    #    deny all;
    #}
#
    #location = /robots.txt {
    #    allow all;
    #    log_not_found off;
    #    access_log off;
    #}
#
    #location ~* \.(css|gif|ico|jpeg|jpg|js|png)$ {
    #    expires max;
    #    log_not_found off;
    #}
#
    #location ~ \.php$ {
    #    try_files $uri =404;
    #    fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #    #fastcgi_pass 127.0.0.1:32000;
    #    fastcgi_pass 127.0.0.1:9000;
    #    fastcgi_index index.php;
    #    include fastcgi_params;
    #    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #    fastcgi_param PATH_INFO $fastcgi_path_info;
    #}
#
    #location = /favicon.ico {
    #    log_not_found off; access_log off;
    #}
#
    #location ~* (\.html|\.htm) {
    #    expires epoch;
    #    add_header Cache-Control "max-age=0, no-cache, no-store, must-revalidate, proxy-revalidate";
    #}
#
    #location ~* (?<!\.html|\.htm)  {
    #    expires 300s;
    #    add_header Cache-Control "public, max-age=300, must-revalidate, proxy-revalidate";
    #}
}

upstream dev-index_php_upstream {
    server 127.0.0.1:8090; # NGINX Unit backend address for index.php with
                           # 'script' parameter
}

upstream dev-direct_php_upstream {
    server 127.0.0.1:8091; # NGINX Unit backend address for generic PHP file handling
}

server {
    listen 80;
    listen [::]:80;

    server_name desenv-site.aluguelnobolso.com.br www.desenv-site.aluguelnobolso.com.br;
    return 301 $scheme://use.aluguelnobolso.com.br$request_uri;

    access_log /var/log/nginx/desenv-site.aluguelnobolso.com.br_access.log;
    error_log /var/log/nginx/desenv-site.aluguelnobolso.com.br_error.log;

    #root /ANB/docker/site/files;
    #index index.php index.html index.htm;
#
    #location ~ /.well-known/acme-challenge {
    #    allow all;
    #    root /ANB/docker/site/files;
    #}
#
    #location / {
    #    try_files $uri $uri/ /index.php$is_args$args;
    #}
#
    #location ~ /\.ht {
    #    deny all;
    #}
#
    #location = /robots.txt {
    #    log_not_found off;
    #    access_log off;
    #    allow all;
    #}
#
    #location ~* \.(css|gif|ico|jpeg|jpg|js|png)$ {
    #    expires max;
    #    log_not_found off;
    #}
#
    #location ~ \.php$ {
    #    try_files $uri =404;
    #    fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #    #fastcgi_pass 127.0.0.1:32000;
    #    fastcgi_pass 127.0.0.1:9000;
    #    fastcgi_index index.php;
    #    include fastcgi_params;
    #    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #    fastcgi_param PATH_INFO $fastcgi_path_info;
    #}
#
    #location ~*.php$ {
    #    try_files $uri =404;
    #    fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #    #fastcgi_pass 127.0.0.1:32000;
    #    fastcgi_pass 127.0.0.1:9000;
    #    fastcgi_index index.php;
    #    include fastcgi_params;
    #    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #    fastcgi_param PATH_INFO $fastcgi_path_info;
    #}
#
    #location = /favicon.ico {
    #    log_not_found off; access_log off;
    #}
#
    #location ~* (\.html|\.htm) {
    #    expires epoch;
    #    add_header Cache-Control "max-age=0, no-cache, no-store, must-revalidate, proxy-revalidate";
    #}
#
    #location ~* (?<!\.html|\.htm)  {
    #    expires 300s;
    #    add_header Cache-Control "public, max-age=300, must-revalidate, proxy-revalidate";
    #}
}
